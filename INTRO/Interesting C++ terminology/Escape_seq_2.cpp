
#include<iostream>
using namespace std;

int main(){
    cout << "C :\\MinGW\\mingw32\\lib\\ldscripts";
    return 0;
}
// we have used double backslash coz one of them is escape sequence character.If we dont use it then it throws compilation error as backslash is an escape sequence and \M is not an escape seq character.So 