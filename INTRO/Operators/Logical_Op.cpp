#include<iostream>
using namespace std;

int main(){
    int a=50,b=0;
    
    cout << "a && b : " << (a&&b) << endl;
    cout << "a || b : " << (a||b) << endl;
    cout << "!a : " << (!a) << endl;
    cout << "!b : " << (!b) << endl;

    cout << "a and b : " << (a and b) << endl;
    cout << "a or b : " << (a or b) << endl;
    cout << "not a : " << (not a) << endl;
    cout << "not b : " << (not b) << endl;

    // we have gates in the form of words too.

    

}